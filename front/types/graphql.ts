

export type Scalars = {
    ID: string;
    String: string;
    Boolean: boolean;
    Int: number;
}

export type Content = {
    id: Scalars['ID'];
    __typename: Scalars['ID'];
    name: Scalars['String'];
    description: Scalars['String'];
}